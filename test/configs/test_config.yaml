random_seed: 3319
device:
    type: cuda
    ordinal: 0
dataset:
    data_root: root
    transforms:
        - name: 'random_crop'
          do: true
          kwargs:
              size: (256, 256)
dataloader:
    batch_size: 128
    drop_last: true
    num_workers: 8
